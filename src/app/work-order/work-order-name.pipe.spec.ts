import { WorkOrderNamePipe } from './work-order-name.pipe';

describe('WorkOrderNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkOrderNamePipe();
    expect(pipe).toBeTruthy();
  });
});
